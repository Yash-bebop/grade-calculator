# Node modules
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Supabase temporary files
supabase/.temp/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS
.DS_Store

# Windows
Thumbs.db

# Admin-only review dashboard — deliberately NOT part of the public repo.
# It's uploaded straight to Supabase Storage (bucket: site-assets) and
# fetched at runtime only for the signed-in admin. See admin/README.md.
admin.js
admin/*.js
!admin/README.md
